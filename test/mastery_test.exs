defmodule MasteryTest do
  use ExUnit.Case
  doctest Mastery

  test "templates generate questions" do
    # assert Mastery.hello() == :world
  end

  test "users answer with responses" do
    # assert Mastery.hello() == :world
  end

  test "quizes ask questions" do
    # assert Mastery.hello() == :world
  end
end
